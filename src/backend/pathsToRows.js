const fs = require('fs');

module.exports = paths => {
    return new Promise((resolver, reject) => {
        try {
            const rows = paths.map(path => {

            })
        } catch (error) {

        }
    })
}